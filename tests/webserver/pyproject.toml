[build-system]
requires = ["setuptools>=62", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "live-nb2service"
version = "0.1.0"
description = "Test environment for dispatcher-plugin-nb2workflow"
requires-python = ">=3.10"
dependencies = [
    "nb2workflow[service,domains,mmoda] >=1.3.115",
    "oda-api"
]

